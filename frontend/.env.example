# API base URL. Leave empty to auto-detect `window.location.origin + '/api'`.
VITE_API_BASE_URL=

# Storage base URL for uploaded images. Leave empty to use `window.location.origin + '/storage'`.
VITE_STORAGE_BASE_URL=
